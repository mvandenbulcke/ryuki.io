# Changelog

All notable changes to this project are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).
Entries are generated from [Conventional Commits](https://www.conventionalcommits.org)
by [git-cliff](https://git-cliff.org).

> **Seed file.** No version tags exist yet, so this changelog has no released
> sections. The release workflow (`.github/workflows/release.yml`) regenerates
> this file from the git history on every `v*` tag. To regenerate locally,
> install git-cliff (`cargo install git-cliff` or `brew install git-cliff`)
> and run:
>
> ```sh
> git-cliff --config cliff.toml --output CHANGELOG.md
> ```
>
> To preview only the next release's section:
>
> ```sh
> git-cliff --config cliff.toml --unreleased --tag v0.1.0 --strip all
> ```

## [Unreleased]

- Release-engineering tooling: changelog (git-cliff) + tagged release workflow.

<!-- generated by git-cliff -->
